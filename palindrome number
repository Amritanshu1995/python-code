# palindrome number programe

n = int(input("enter the number: "))
x = n
rev = 0
while(n>0):
    rev = (rev * 10) + n % 10
    n = n // 10
if (x == rev):
    print(f"the given number {x} is palindrome")

else:
    print(f"the given number {x} is not palindrome")
